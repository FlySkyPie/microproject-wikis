Switch Node 會依序將 `variable` 的值與各個 `case_N` 進行比較：

1.  **匹配成功**：執行與該 `case` 對應的子節點。
2.  **無匹配項**：執行最後一個子節點（Default 分支）。
3.  **狀態切換**：如果原先匹配的子節點正在 `RUNNING`，但在下一個 Tick 時 `variable` 改變導致匹配到不同分支，原有的子節點會先被 **Halted**（中止），然後才執行新的分支節點。