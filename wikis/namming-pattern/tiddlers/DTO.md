在程式設計領域中，資料傳輸物件（Data Transfer Object，簡稱 DTO）是一種在進程（Process）之間傳遞資料的物件。使用 DTO 的主因在於進程間的通訊通常仰賴遠端介面（例如 Web Service），而每一次的呼叫都是一項昂貴的資源消耗。由於每次呼叫的主要成本與用戶端及伺服器之間的往返時間（Round-trip time）有關，因此減少呼叫次數的一種方法，就是使用一個物件（即 DTO）來彙整原本需要透過多次呼叫才能傳輸的資料，並改由單次呼叫來提供服務。