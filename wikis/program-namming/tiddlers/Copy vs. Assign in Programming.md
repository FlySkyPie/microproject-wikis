在描述程式碼中的賦值行為（如 PHP 的 `$a=&$b;`）時，命名的選擇取決於視角：
- **Assign**：從指派運算子 (Assignment operator) 的層級描述。
- **Copy**：從實作細節看，若涉及讀取一個 reference 的「值」並寫入另一個空間，即是「抄寫」的動作。
- **Duplicate**：若要強調「原本已有 $b，現在多產生一個相等的 $a」且可能涉及維護兩份資料的成本時使用。