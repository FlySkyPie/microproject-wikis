在程式命名中，「計算」一詞的選擇可依據計算的性質決定：

* **calculate**
  * 定義：能用「計算機 (calculator)」算出來的數學運算。
  * 特性：常具備「策劃、推測」的語意。
  * 範例：`a calculated decision`。
* **compute**
  * 定義：特指用「電腦 (computer) / 圖靈機」進行的計算。
  * 特性：強調儲存與處理數據，符合邏輯運算。
  * 範例：`This does not compute.` (這不合邏輯)。