兩者在程式命名與商務邏輯中有語意上的差別：

* **sum**
  * 語意：以「加法」算出來的純數學總和。
  * 範例：`2 + 3 + 5 = 10`。
  * 應用：數學函式（如 Excel 的 `SUM`），適合單純累加的變數名。
* **total**
  * 語意：由「一連串複合計算」（含折扣、加乘等）得到的最終總數。
  * 範例：`原價 -> 打折 -> 總計`。
  * 應用：電商結帳（`subtotal`, `total`），或複合傷害計算（`totalDamage`）。亦常作為形容詞使用。