當命名的目的是為了強調事物性質的「區分」或「階級隔離」時，適合使用 `type` 或 `class`。

* **Type**：常用於對應「型」。例如：血型 (`bloodType`)、統計學的第一型/第二型錯誤 (`type I / II error`)。
* **Class**：除了種類，另有「級別」的含意。例如：艙等 (`first class`)、社會階級 (`middle class`)。

在程式設計中，這兩個字常是保留字，建議使用組合字（如 `ShipClass`）或在支援的語言中使用特殊前綴（如 C# 的 `@class`）來避開。