在特定領域中，應優先使用該領域的慣用術語以提昇可讀性：

* **藝術作品**（電影、音樂、遊戲）：使用 `genre`。
* **車輛**：使用 `make`（品牌）、`model`（車系）、`bodyStyle`（車型）。
* **生物學**：使用 `species`（物種）、`breed`（品種）、`strain`（品系）。
* **烹飪**：使用 `doneness`（熟度）、`cut`（部位）。
