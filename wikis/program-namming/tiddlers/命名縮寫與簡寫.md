- 定義：在程式碼中使用較短的符號代替完整的單字。
- 案例：
  - `nums` 代表 `numbers`。
  - `qty` 代表 `quantity`。
  - `cnt` 代表 `count`。
- 爭議點：縮寫究竟是代表 [Brevity](<#Brevity>)（簡潔）還是 [Obscurity](<#Obscurity>)（隱晦），取決於讀者的 [知覺負載](<#知覺負載>)。
- 評量準則：應根據「程式碼要給誰看」以及「開發團隊的共同記憶」來決定。