- **Delete**
  - 定義：將東西從存在變為消滅（存在 -> 無）。
  - 應用場景：SQL 刪除資料、Windows 刪除檔案、C++ 銷毀物件、HTTP 動詞。
  - 特性：多半無法取代 `remove` 來表達從容器移除物件。
- **Remove**
  - 定義：將物件從容器中移走，亦可表達刪除。
  - 應用場景：從好友名單移除帳號、從集合（Collection）中移除元素。
  - 特性：具備較強的替代性，常可用來取代 `delete`（如 Linux 的 `rm` 指令）。